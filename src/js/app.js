const regularPurchases = 3000000
const regularPurchasesPersent =  0.01
const cashback = regularPurchases * SpeechRecognitionResult
console.log(cashback)
if (regularPurchases * regularPurchasesPersent > 3000){
    const cashback = 3000;
}
else{
    const cashback = regularPurchases * regularPurchasesPersent;
}